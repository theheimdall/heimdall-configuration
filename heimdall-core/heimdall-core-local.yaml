spring:
  cloud:
    gateway:
      routes:
        - id: book-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/book/**
server:
  port: 9000

management:
  endpoints:
    web:
      exposure:
        include: "*"
